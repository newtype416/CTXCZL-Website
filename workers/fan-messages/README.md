# Cloudflare Workers deployment files

This directory stores the anonymous fan-message API used by the website.

Run the commands in `../../CLOUDFLARE_DEPLOYMENT.md` after creating a Cloudflare account.
